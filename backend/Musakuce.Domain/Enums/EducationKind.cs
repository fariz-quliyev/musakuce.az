namespace Musakuce.Domain.Enums;

/// <summary>"Təhsil" — Phase 13 Part 1. One flat, filterable kind covers
/// every category the spec lists rather than separate Teacher/Graduate/
/// School tables.</summary>
public enum EducationKind
{
    SchoolHistory,
    ImportantDate,
    FirstSchool,
    FirstTeacher,
    NotableTeacher,
    NotableGraduate,
    Achievement,
    Institution,
    Story,
    Other,
}
